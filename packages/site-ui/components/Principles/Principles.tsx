import React from "react";

import { classNames } from "@breadchain.xyz/utils";
import { pageWrap } from "../classStrings";

export function Principles() {
  // TODO everything
  return <section className={classNames(pageWrap)}>principles</section>;
}
