import React from "react";

import { classNames } from "@breadchain.xyz/utils";
import { pageWrap } from "../classStrings";

export function CTASection() {
  // TODO everything
  return <section className={classNames(pageWrap)}>bake with us</section>;
}
